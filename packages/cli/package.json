{
  "name": "@mini-architecture/cli",
  "version": "0.0.7-beta.2",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "rm -rf dist && tsc -w",
    "build": "rm -rf dist && tsc && yarn copy",
    "copy": "cp -R ../android ./android && rm -rf ./android/.gradle ./android/app/build",
    "lint": "tsc --noEmit --skipLibCheck"
  },
  "bin": {
    "ma-cli": "bin/index.js"
  },
  "files": [
    "dist"
  ],
  "author": "lawler61",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lawler61/mini-architecture.git"
  },
  "dependencies": {
    "@mini-architecture/devtools": "^0.0.2-beta.2",
    "@mini-architecture/framework": "^0.0.6-beta.0",
    "@mini-architecture/mini": "^0.0.5",
    "@mini-architecture/pack": "^0.0.7-beta.2",
    "@mini-architecture/utils": "^0.0.1",
    "chalk": "^4.1.0",
    "commander": "^6.2.1",
    "fs-extra": "^9.1.0",
    "ignore-by-default": "^2.0.0"
  }
}
