{
  "name": "@mini-architecture/devtools",
  "version": "0.0.1-beta3",
  "description": "",
  "main": "dist/server/launch.js",
  "scripts": {
    "pack:mini": "rm -rf dev/mini && ../cli/bin/index.js pack -e @mini -m dev -p devtools -o dev/mini",
    "dev:client": "yarn pack:mini && rm -rf dev/client && scripts/client.ts",
    "dev:launch": "scripts/launch.ts",
    "build:client": "parcel build client/index.html -d dist/client --no-cache",
    "build:server": "rm -rf dist && tsc -p .",
    "build": "yarn build:launcher && yarn build:client"
  },
  "author": "lawler61",
  "license": "ISC",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lawler61/mini-architecture.git"
  },
  "dependencies": {
    "chrome-launcher": "^0.13.4",
    "chrome-remote-interface": "^0.28.2",
    "ejs": "^3.1.3",
    "express": "^4.17.1",
    "express-ws": "^4.0.0",
    "get-port": "^5.1.1",
    "glob": "^7.1.6",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "^4.0.0",
    "less": "^3.12.2",
    "open": "^7.3.0",
    "parcel-bundler": "^1.12.4",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-modal": "^3.11.2"
  }
}
